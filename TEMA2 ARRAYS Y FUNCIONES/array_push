<?php

$my_array = array("foo" => 1, "hello" => "world");

$allowed = array("foo", "bar");

$new=array();
foreach ($my_array as $clave => $valor) {
	if ($clave == $allowed[0] || $clave == $allowed[1]){
	array_push($new, $clave);
	}		
	
}
	print_r($new);

?>
